import React from 'react'
import {View, Text, StyleSheet} from 'react-native'

import Heading from '../../components/Typo/Heading'

const User = props => {
	return (
		<View>
			<Heading lvl={1} text="User Screen"/>
		</View>
	)
}

const styles = StyleSheet.create({

})

export default User