import React from 'react'
import {View, Text, StyleSheet} from 'react-native'

import Heading from '../../components/Typo/Heading'

const History = props => {
	return (
		<View>
			<Heading lvl={1} text="History Screen"/>
		</View>
	)
}

const styles = StyleSheet.create({

})

export default History