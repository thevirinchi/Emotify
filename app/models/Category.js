class CategoryModel {
	constructor(id, icon, title, bgColor, fgColor) {
		this.id = id;
		this.icon = icon;
		this.title = title;
		this.bgColor = bgColor;
		this.fgColor = fgColor;
	}
}

export default CategoryModel;